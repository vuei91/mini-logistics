# CJ대한통운 '더운반' 백엔드 채용 대비 미니프로젝트 설계 가이드

> 참고 공고: [CJ대한통운 더운반디지털플랫폼 백엔드개발 경력](https://career.rememberapp.co.kr/job/posting/297682)

## 1. 공고 핵심 요약

- **도메인**: 물류 플랫폼 - 미들마일(화주/차주 매칭, 배차), 온디맨드(수요응답형 매칭)
- **핵심 업무**: 매칭/배차 알고리즘 API 설계·개발, 프론트-백엔드 인터페이스 API
- **필수 기술**: Java 21, Spring Boot 3.x (Boot/Batch/JPA), REST API, PostgreSQL, RabbitMQ(비동기/이벤트)
- **우대 기술**: AWS/K8S/Docker, NoSQL(Redis, MongoDB), 테스트코드(JUnit/Mockito), 모빌리티 도메인 경험

→ 이 공고는 **"매칭 + 배차 + 비동기 이벤트 처리"**를 핵심 역량으로 본다. 포트폴리오는 이 3가지를 명확히 드러내야 한다.

## 2. 미니프로젝트 컨셉 제안

**"화주-차주 매칭 및 배차 시스템 (Mini 더운반)"**

화주가 운송 요청을 등록하면, 조건(위치/차량 타입/시간/선호구간)에 맞는 차주를 매칭하고, 동적 운임을 산정해 배차하는 시스템.

### 핵심 시나리오
1. 화주가 운송 요청(출발지, 도착지, 차량타입)과 하나 이상의 화물 품목(설명·중량)을 등록
2. 시스템이 화물 품목의 **총중량**, 차량타입, 지역 및 **차주 선호구간**에 맞는 대기 차주 목록을 조회하고 매칭 점수를 계산
3. **동적 운임 산정 엔진**이 거리/시간대/노선 수요/계절 가중치를 반영해 운임 계산
4. 차주에게 배차 제안 → 수락/거절 처리 (온디맨드 매칭)
5. 배차 확정 시 상태 변경 이벤트를 비동기로 발행 (알림, **정산** 등 후속 처리)
6. 운송 상태(배차완료→운송중→완료) 추적 API 제공, 완료 시 **정산 내역 자동 생성**

### 실제 서비스 기능 매핑 (더운반 벤치마킹)

| 더운반 실제 기능 | 미니프로젝트 채택 범위 | 이유 |
|---|---|---|
| AI 최적 운임 제안 (기상/유가/계절/수요 분석) | **동적 프라이싱 엔진**으로 다운스코프: 거리×기본단가 + 시간대/노선수요/계절 가중치 규칙 기반 계산 | 외부 데이터 연동·ML 모델은 스택(Java/Spring)과 무관하고 시간 대비 비효율. 전략 패턴으로 설계해 향후 ML 교체 가능성을 어필 |
| 스마트 자동 배차 (화물 정보 + 차주 선호구간 매칭) | **그대로 채택**: `Driver.preferredRoutes` 속성 추가, 매칭 스코어에 가중치 반영 | 기존 매칭 로직에 자연스럽게 확장되고 실제 서비스와 유사도가 높음 |
| 통합 운송 관리 (주문접수~실시간관제~정산) | **P3 후순위로 실시간 트래킹 + 정산 둘 다 채택**: 트래킹은 Redis + SSE, 정산은 `COMPLETED` 이벤트 → Consumer → `Settlement` 생성 | 실시간 트래킹은 SSE 기반으로 스코프를 최소화하면 백엔드 차별화 포인트(WebSocket/스트리밍/Redis GEO) 확보 가능. 둘 다 P1/P2 완주 후 착수 |

## 3. 설계 시 중점 포인트

### 3.1 도메인 모델링 (가장 중요)
- `Shipper`(화주), `Driver`(차주), `Vehicle`(차량), `ShipmentRequest`(운송요청), `CargoItem`(화물 품목), `Dispatch`(배차)
- `ShipmentRequest 1 : N CargoItem`으로 모델링한다. 운송 요청은 출발·도착지·요구 차량·상태를, 화물 품목은 설명과 중량을 책임진다. 요청의 총중량은 품목 중량 합계로 계산하며, 차량 적재량 매칭에 사용한다.
- 초기에는 화물 품목이 운송 요청에 종속되어 독립 라이프사이클이 없으므로 `ShipmentRequest` Aggregate 내부 엔티티로 관리한다. 향후 화물별 파손·분할 운송·개별 추적이 필요해져도 확장할 수 있다.
- 상태(State)를 명확히 관리: `REQUESTED → MATCHING → DISPATCHED → EN_ROUTE_TO_PICKUP → PICKED_UP → IN_TRANSIT → COMPLETED / CANCELED`
  - `DISPATCHED`: 차주 수락 완료 (아직 이동 전)
  - `EN_ROUTE_TO_PICKUP`: 차주가 픽업지로 이동중
  - `PICKED_UP`: 픽업지 도착 및 화물 상차 완료
  - `IN_TRANSIT`: 목적지로 이동중
  - `COMPLETED`: 배송 완료
- 상태 전이는 엔티티 내부 메서드로 캡슐화 (도메인 주도 설계 어필)

### 3.2 매칭/배차 알고리즘
- 단순 필터링(지역, 차량타입, 가용시간)부터 시작 → 거리 기반 스코어링으로 확장
- **차주 선호구간(`preferredRoutes`) 매칭 가중치** 추가: 선호 구간과 일치할수록 매칭 점수 상승
- 배차 로직은 별도 서비스/도메인으로 분리 (`DispatchService`, `MatchingStrategy` 인터페이스화)
- 알고리즘을 교체 가능한 구조로 설계하면 "설계 역량"을 어필하기 좋음

### 3.3 동적 운임 산정 엔진 (다운스코프된 "AI 운임 제안")
- "AI"라 칭하지 않고 **규칙 기반 동적 프라이싱**으로 정직하게 설계
- 계산식: `기본운임(거리×단가) × 시간대가중치 × 노선수요가중치 × 계절가중치`
  - 시간대가중치: 피크/오프피크 시간대별 배율
  - 노선수요가중치: 최근 N시간 내 해당 노선(출발-도착) 요청 건수 기반 배율
  - 계절가중치: 설정 테이블(계절/월별) 기반 배율
- `PricingStrategy` 인터페이스로 분리 → 나중에 실제 데이터 기반(ML) 전략으로 교체 가능한 구조임을 어필
- 노선수요 집계는 Redis(카운터) 또는 DB 집계 쿼리로 구현 가능 (우대사항 NoSQL 경험과 연결)

### 3.4 REST API 설계
- 리소스 중심 설계: `/shipment-requests`, `/drivers/{id}/dispatches`, `/dispatches/{id}/accept`
- 요청/응답 DTO 분리, 검증(Bean Validation), 에러 응답 표준화(공통 예외 핸들러)
- Swagger/OpenAPI 문서화 (실무에서 프론트 연동 고려한 습관 어필)

### 3.5 비동기/이벤트 처리 (RabbitMQ) — 차별화 포인트
- 배차 확정, 상태 변경 시 이벤트 발행 → Consumer가 알림/로그/정산 처리
- 예: `DispatchConfirmedEvent` → Exchange → Queue → 알림 Consumer
- 트랜잭션과 메시지 발행 분리 고려 (Transactional Outbox 패턴까지 다루면 강점)

### 3.6 실시간 위치 트래킹 (P3 후순위)
- 차주가 픽업지/목적지로 이동하는 동안 화주가 현재 위치를 실시간 조회
- **차주 → 서버**: `POST /dispatches/{id}/location` (좌표 + 타임스탬프). 앱이 5~10초 간격 or 최소 이동거리 임계값 만족 시 전송
- **저장 전략**:
  - 현재 위치: **Redis** (Key: `dispatch:{id}:location`, TTL 30초) 또는 Redis GEO 명령
  - 경로 이력: 필요 시 PostgreSQL time-series 테이블 (배송 완료 후 통계용)
- **서버 → 화주 푸시**: **SSE(Server-Sent Events)** 채택 (단방향, WebSocket/STOMP 대비 구현 단순)
  - 엔드포인트: `GET /dispatches/{id}/location/stream` (Content-Type: `text/event-stream`)
  - Spring `SseEmitter` 또는 `Flux<ServerSentEvent<T>>` (WebFlux) 사용
  - RabbitMQ를 스트림 채널로 쓰는 방식은 스코프 대비 복잡도가 커서 이 프로젝트에서는 채택하지 않음. 배차/픽업/완료 도메인 이벤트는 여전히 RabbitMQ 사용.
- **인가**: JWT/세션에서 화주 ID 추출 → 해당 배차의 화주 본인만 스트림 구독 가능
- **레이트 리미팅**: 서버측에서 최소 인터벌(예: 5초) or 최소 이동거리(예: 20m) 미만이면 저장/브로드캐스트 스킵
- **테스트**: 좌표 스트림 mock으로 상태별 브로드캐스트 검증

### 3.7 정산(Settlement) 모듈 (P3 후순위, 다운스코프된 "통합 운송 관리")
- 운송 완료(`COMPLETED`) 이벤트를 Consumer가 수신 → `Settlement` 엔티티 생성(운임, 수수료, 정산상태)
- 정산 상태: `PENDING → CONFIRMED → PAID` 흐름으로 관리
- 실시간 관제(위치추적)는 제외하되, 필요하면 `Dispatch`에 `currentLocation` 필드만 두고 단순 PATCH API로 흉내만 내는 수준으로 최소화
- "주문(요청) → 매칭 → 배차 → 운송 → 정산"까지 엔드투엔드 흐름을 보여주는 것이 핵심

### 3.8 데이터베이스
- PostgreSQL 기반, JPA/Hibernate 매핑
- 동시성 이슈 고려: 같은 차주에게 중복 배차되지 않도록 락(비관적 락 or unique 제약) 처리
- 인덱스 설계(지역/상태 컬럼) 및 N+1 문제 방지(fetch join, batch size)

### 3.9 테스트 코드
- JUnit5 + Mockito로 서비스 계층 단위 테스트
- 매칭 알고리즘, 운임 산정 로직, 상태 전이 로직은 반드시 테스트 케이스로 검증 (도메인 로직 신뢰성 어필)

### 3.10 인프라/운영 (우대사항 커버)
- Docker Compose로 로컬 환경 구성(App + PostgreSQL + RabbitMQ + Redis)
- Redis는 캐싱(가용 차주 목록) 또는 분산 락(중복 배차 방지)에 활용
- 가능하면 간단한 AWS 배포(EC2/ECS) 또는 K8S manifest 작성 경험 추가

## 4. 포트폴리오 어필 전략

- README에 **"공고 요구사항 → 프로젝트 구현 매핑표"**를 넣어 지원 포지션과의 연결성을 명시
- 배차 알고리즘의 설계 고민(트레이드오프)을 문서화 (예: 단순 규칙 vs 스코어링 방식 비교)
- 동시성/이벤트 처리 등 "실무형 문제"를 의도적으로 넣고 해결 과정을 기록
- API 명세서(Swagger) + ERD + 아키텍처 다이어그램 첨부

## 5. 우선순위 (시간이 부족할 경우)

1. 도메인 모델 + 상태 전이 + REST API (필수)
2. 매칭(선호구간 포함)/배차 로직 + 테스트 코드 (필수)
3. RabbitMQ 이벤트 처리 + 정산 모듈 (차별화)
4. 동적 프라이싱 엔진 (여유 있으면 추가, 없으면 고정 단가로 대체 가능)
5. Redis 캐싱/락, Docker, AWS/K8S (여유 있으면 추가)
